/* =========================================================
   ABOUT PAGE — page-specific behaviour
   Journey timeline, counters, and scroll-reveal are handled
   generically by js/global.js (via .reveal / [data-counter]).
   This file is reserved for any About-page-only interactivity
   that gets added later, so the page has its own script file
   per the project's module structure.
   ========================================================= */
(function(){
  "use strict";
  // Intentionally empty for now — see js/global.js for shared behaviour.
})();
